# ASCII-only, same reason as sdkconfig.defaults: parsed by IDF Python tooling
# using the locale encoding. The CJK font needs flash, so the default 1MB app
# partition is not enough -- 3M here.
# Name,   Type, SubType, Offset,   Size,    Flags
nvs,      data, nvs,     0x9000,   0x6000,
phy_init, data, phy,     0xf000,   0x1000,
factory,  app,  factory, 0x10000,  3M,
