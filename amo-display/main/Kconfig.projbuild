# 必须放在组件目录（这里是 main/）。IDF 只从各组件收集 Kconfig.projbuild，
# 不扫工程根目录 —— 放错地方的症状是编译期报 CONFIG_XXX undeclared，
# 而 menuconfig 里也根本看不到这个菜单。

menu "Agent Display"

config AGENT_WIFI_SSID
    string "WiFi SSID"
    default "your-ssid"

config AGENT_WIFI_PASSWORD
    string "WiFi 密码"
    default "your-pass"

config AGENT_BUZZER_GPIO
    int "外接无源蜂鸣器 GPIO（-1 关闭）"
    default -1
    help
        本板（T-Display S3）没有板载蜂鸣器。要加就接一个空闲脚。
        注意 39-48 几乎都被 LCD 并口数据线占了，可用的空闲脚不多，
        GPIO16/17/18 是候选。接之前先对照 spec 5.1 的引脚表确认没冲突。

config AGENT_TINY_FULL_DETAIL
    bool "请求完整命令参数（?d=full）"
    default n
    help
        默认只取工具名。桌面小屏是公开展示的，别人走过就能看见。

config AGENT_DUMP_FRAME_ON_BOOT
    bool "开机后 dump 一帧屏幕内容到串口"
    default n
    help
        把 LVGL 画好的整屏以 base64 的 RGB565 吐到串口，主机侧可还原成 PNG。
        开发时人看不到屏幕（远程、或板子不在手边）就靠它验证排版 ——
        "UI 长什么样"因此从只能靠肉眼的问题变成可自动检查的问题。
        正常固件不需要，默认关闭：一帧约 145KB base64，会把串口刷屏十几秒。

config AGENT_UI_DEMO
    bool "UI 演示模式：轮流展示五种状态"
    default n
    help
        忽略真实会话，每 4 秒切一种状态，循环展示。
        用途有二：一是验证 UI —— 五种状态里 idle 与 nolink 在真实环境里
        很难凑出来（只要你还在敲代码，就不会有 idle）；二是给人看这只
        小怪物时不必等它恰好进入某个状态。
        配合 AGENT_DUMP_FRAME_ON_BOOT 可以一次抓齐五张图。

endmenu
