# ESP32 桌面显示器 Implementation Plan

> ## ⚠ 2026-08-19 修订：硬件型号原先写错了
>
> 本计划最初整篇基于 **T-Display-AMOLED-Lite**（SH8501B0 / QSPI / 194×368 / AXP2101）。
> 实测确认手上的板子是 **LilyGo T-Display S3**：ST7789 LCD、8 位 I8080 并口、
> 170×320（横屏 **320×170**）、**无 PMU、无触摸、无 RGB 灯**。
>
> **权威以 spec §5.1 为准**，那里记录了实测确认过的引脚与三个软件坑。
> 本文档中所有 194×368 / QSPI / AXP2101 / WS2812 / GPIO21 的描述均已作废。
>
> 受影响的任务：
>
> | 任务 | 状态 |
> |---|---|
> | Task 0 环境探针 | **已作废**。结论是"IDF 5.2.3 能编译 LilyGo-Display-IDF"，而我们不再需要那个仓库。保留作历史记录。 |
> | Task 1 `tiny_parse` | ✅ 完成，不受影响（纯逻辑，不碰硬件） |
> | Task 2 主机表 | ✅ 完成，不受影响 |
> | Task 3 工程骨架 | ✅ **已按 T-Display S3 重做**，产物见 `components/tds3_board/`。本文档里的 Task 3 步骤已过时。 |
> | Task 4 中文字库 | ✅ 完成。屏高 194→170，28px 状态字可能需重生成 24px 一份。 |
> | Task 5 文案防漂移 | ✅ 完成，不受影响 |
> | Task 6 WiFi/mDNS | 不受影响 |
> | Task 7 配对与 NVS | 按键 GPIO21 → **GPIO14** |
> | Task 8 轮询聚合 | 不受影响 |
> | Task 9 横屏 UI | **布局尺寸全部要改**：368×194 → 320×170（宽高比几乎不变，按 0.87 缩放）。WS2812 告警全部删除。亮度分档要改用 LEDC 给 GPIO38 输出 PWM —— LCD 画黑不省电，原设计"AMOLED 黑像素不通电"的前提不成立。 |
> | Task 10 清理与文档 | 需一并清掉本计划里的 AMOLED 残留 |

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 一块放在桌角、余光扫一眼就知道"要不要走过去"的 ESP32 显示器，通过 mDNS 发现宿主机、配对后轮询 `/api/tiny`，用发光面积和颜色编码四种状态。

**Architecture:** 板级层用 ESP-IDF 内置的 `esp_lcd`（ST7789 + i80 是官方支持），
`components/tds3_board` 只有引脚定义与初始化，**无 vendored 代码**。其余全部自己写：
解析、主机表、发现、配对、轮询、UI 各占一个聚焦文件。所有纯逻辑（三行解析、主机表增删、状态判定）不依赖 ESP 头文件，因此能用主机 gcc 编译成可执行文件直接跑测试，不必等硬件。

**Tech Stack:** ESP-IDF v5.2.3（内置 esp_lcd）、LVGL 8.3.11（component manager）、esp_http_client、mdns、nvs_flash、lv_font_conv（node）

**Spec:** `docs/superpowers/specs/2026-08-18-agent-dashboard-design.md`（§5 是本计划的权威）

## Global Constraints

以下取值来自 spec 与实测，每个任务的要求都隐含包含本节。

### 硬件事实（2026-08-19 在实物上实测确认，权威见 spec §5.1）

- **LilyGo T-Display S3**，ESP32-S3R8 / 16MB Flash / 8MB OPI PSRAM
- 屏驱 **ST7789**，物理 170×320，**本项目用横屏 320×170**
- **8 位 I8080 并口**：D0-D7 = 39,40,41,42,45,46,47,48；WR=8, RD=9（须常高）, DC=7, CS=6, RST=5，pclk 10MHz
- **总电源使能 GPIO15**（不拉高屏幕完全不工作）；**背光 GPIO38**（纯开关，调光需 LEDC PWM）
- 面板列偏移 35（面板 170 宽、控制器 240 宽），横屏后落在 y 轴
- 按键：GPIO0 是 BOOT strapping 脚，**不用**；用 **GPIO14**
- **无 PMU、无触摸、无 RGB 灯、无蜂鸣器**

三个软件坑（详见 spec §5.1 与 `components/tds3_board/tds3_board.c` 的注释）：
颜色字节序须设 `.flags.swap_color_bytes`、`esp_lcd_panel_draw_bitmap()` 是异步的、
IDF 自带 st7789 驱动需补厂商初始化表。另：`sdkconfig.defaults` 与 `partitions.csv`
必须纯 ASCII，否则中文 Windows 上 `kconfgen` 直接炸。

### 构建环境（已实测接通）

```powershell
$env:IDF_PATH       = "C:\Users\CC\esp\v5.2\esp-idf"                                        # v5.2.3
$env:IDF_TOOLS_PATH = "D:\Lvwenchao\geek\esp\Espressif\frameworks\esp-idf-v5.2.1\tools"
& "$env:IDF_PATH\export.ps1"
```

### 语义色与文案（与宿主端逐字一致）

- run `#3FBFD8` / wait `#FFB020` / idle `#4A5B78` / stale `#E2564A`
- 状态短文案取自 `phrases.py:STATE_LABEL_SHORT`：waiting `该你了`／running `干着呢`／idle `摸鱼中`／stale `没声儿了`
- 连不上任何主机是**第五种**显示态，文案 `连不上`，与 `没声儿了`（服务端说某会话失联）语义不同，不得合并

### 协议

- `GET /api/tiny?k=<token>` 返回严格三行：`版本|w,r,i|主机名` / `会话名\tdetail` / `cpu,mem,gpu,net_kb`
- 无 GPU 时 gpu 为 `-1`；第二行无会话时为空行
- `?d=full` 才给完整参数，默认脱敏到工具名；**任何情况下都不下发 cwd**
- `POST /api/pair` 仅在宿主开启的 60 秒窗口内返回 `{"token","host_id","host"}`，否则 403
- mDNS 服务类型 `_agentdash._tcp.local.`，TXT 只含 `v` / `host` / `id`

### 亮度与告警

- 空闲 20/255、运行 60/255、等待 255/255 + 呼吸；AMOLED 黑像素不通电，空闲态功耗接近熄屏
- 告警仅在**进入** waiting 的那一次触发，不在持续期间反复
- 无蜂鸣器：告警用 WS2812 琥珀呼吸 + 屏幕拉满。可选外接无源蜂鸣器 `CONFIG_AGENT_BUZZER_GPIO`，默认 `-1`（关闭），建议 GPIO17

## 文件结构

| 文件 | 职责 |
|---|---|
| `amo-display/main/tiny_parse.{c,h}` | 三行文本解析，纯函数，无 ESP 依赖 |
| `amo-display/main/hosts.{c,h}` | 已配对主机表的内存结构与增删查，纯函数 |
| `amo-display/main/hosts_nvs.{c,h}` | 主机表的 NVS 持久化（唯一碰 ESP API 的部分） |
| `amo-display/main/net_wifi.{c,h}` | WiFi 连接 |
| `amo-display/main/discovery.{c,h}` | mDNS 查询 `_agentdash._tcp` |
| `amo-display/main/pairing.{c,h}` | 向开着窗口的主机索取令牌 |
| `amo-display/main/poller.{c,h}` | 轮询全部已配对主机，产出聚合视图 |
| `amo-display/main/buttons.{c,h}` | GPIO21 短按/长按识别 |
| `amo-display/main/main.c` | 启动装配 |
| `amo-display/components/lilygo_board/` | 裁自官方：SH8501B0 QSPI + AXP2101 + WS2812 |
| `amo-display/components/agent_ui/ui.{c,h}` | 状态机与横屏布局 |
| `amo-display/components/agent_ui/ui_strings.h` | 中文文案，与 `phrases.py` 对齐 |
| `amo-display/components/agent_ui/fonts/` | `lv_font_conv` 产物，签入仓库 |
| `amo-display/host_test/` | 主机 gcc 编译的纯逻辑测试 |
| `tests/test_firmware_copy.py` | 宿主仓库里的防漂移测试（固件文案 vs `phrases.py`） |

---

### Task 0: 环境接通与官方仓库编译探针

**Files:**
- Create: `amo-display/README.md`
- Test: 手工（编译产物）

**Interfaces:**
- Consumes: 无
- Produces: 一个确定的答案——现有 IDF v5.2.3 能否编译官方仓库；以及一份可复制的环境配置

> **前置阻塞：C 盘必须有至少 3GB 可用空间。** 实测时 C 盘 0 字节可用，导致探针连续失败三次：
> 编译产物写不下、组件管理器下载 lvgl 写不下临时文件、zip 解压写不下缓存。
> IDF 组件管理器的缓存固定在 `%LOCALAPPDATA%\Espressif\ComponentManager`（当时已占 262MB），
> **无法通过环境变量绕开到别的盘**。开工前先确认 `Get-PSDrive C` 的 Free 大于 3GB。

- [ ] **Step 1: 确认磁盘**

```powershell
Get-PSDrive C | Select-Object @{n='FreeGB';e={[math]::Round($_.Free/1GB,2)}}
```

Expected: FreeGB ≥ 3。不足则停下清理，不要继续。

- [ ] **Step 2: 克隆官方参考仓库到 D 盘**

放 D 盘是因为 C 盘空间紧张，且构建产物有 1GB 量级。

```powershell
New-Item -ItemType Directory -Force -Path D:\Lvwenchao\geek\esp-probe | Out-Null
cd D:\Lvwenchao\geek\esp-probe
git clone --recurse-submodules --depth 1 https://github.com/Xinyuan-LilyGO/LilyGo-Display-IDF.git idf-ref
```

`--recurse-submodules` 不能省：`components/XPowersLib` 与 `components/SensorLib` 是子模块，
缺了会报 `fatal error: XPowersAXP2101.tpp: No such file or directory` 和
`TouchDrvCHSC5816.hpp: No such file or directory`，看起来像编译器问题，其实是文件根本不在。

- [ ] **Step 3: 接通 IDF 环境**

```powershell
$env:IDF_PATH       = "C:\Users\CC\esp\v5.2\esp-idf"
$env:IDF_TOOLS_PATH = "D:\Lvwenchao\geek\esp\Espressif\frameworks\esp-idf-v5.2.1\tools"
& "$env:IDF_PATH\export.ps1"
idf.py --version
```

Expected: 打印版本号，无报错。这台机器的 IDF 源码在 C 盘、工具链在 D 盘，两个变量必须都设。

- [ ] **Step 4: 按官方 setup.py 的等价步骤配置**

官方 `setup.py` 是交互式菜单且只在 Ubuntu 测过，本项目不用它。它做的事等价于下面四条，
**顺序不能变**——先放 sdkconfig 再 set-target，再加依赖，最后必须 reconfigure：

```powershell
cd D:\Lvwenchao\geek\esp-probe\idf-ref
Copy-Item sdkconfig.defaults.t-amoled-lite sdkconfig.defaults -Force
idf.py set-target esp32s3
idf.py add-dependency "lvgl/lvgl^8.3.11"
idf.py reconfigure
```

`reconfigure` 不能省：CMake 的组件列表在第一次 configure 时冻结，子模块和
`main/idf_component.yml` 之后落地的话不会被重新扫描，症状是明明文件在磁盘上却报找不到。

- [ ] **Step 5: 编译**

```powershell
idf.py build
```

Expected: `Project build complete.`

若失败，先按下列顺序排除，这三条都是实测踩过的：

| 症状 | 原因 | 处置 |
|---|---|---|
| `No space left on device` | C 盘满 | 回 Step 1 |
| `fatal error: XPowersAXP2101.tpp` 等 | 子模块没拉 | `git submodule update --init --depth 1` 后 `idf.py fullclean` |
| `fatal error: lvgl.h` 且无 `managed_components/` | 没 reconfigure | `idf.py fullclean; idf.py reconfigure` |

**若排除以上三条后仍失败，且报错指向 IDF API 不兼容**，则记录完整报错，改用 IDF 5.3：
官方 README 声明依赖 v5.3.0。实测 v5.2.3 已能把 992 个目标的 IDF 核心全部编过
（esp_netif / lwip / wpa_supplicant / freertos，并生成了 bootloader.bin），
故倾向认为 5.3 是"我们在这上面测的"而非"用了 5.3 独有 API"，但这一步是最终判据。

- [ ] **Step 6: 记录结论**

把结论写进 `amo-display/README.md`：

```markdown
# ESP32 端

## 构建环境

本机 IDF 源码与工具链分处两盘，必须同时设置：

```powershell
$env:IDF_PATH       = "C:\Users\CC\esp\v5.2\esp-idf"          # v5.2.3
$env:IDF_TOOLS_PATH = "D:\Lvwenchao\geek\esp\Espressif\frameworks\esp-idf-v5.2.1\tools"
& "$env:IDF_PATH\export.ps1"
```

IDF 组件管理器缓存固定在 `%LOCALAPPDATA%\Espressif\ComponentManager`，
无法改盘符，所以 **C 盘需要至少 3GB 可用空间**，否则 lvgl 下载会失败。

## 官方仓库探针结论

把 Step 5 的实际结果写在这里：编译是否通过、若失败则贴出决定性报错、
以及最终采用的 IDF 版本。这一段是后续所有任务的前提，不能空着。
```

- [ ] **Step 7: Commit**

```bash
git add amo-display/README.md
git commit -m "docs: ESP32 构建环境与官方仓库编译探针结论"
```

---

### Task 1: 纯逻辑测试台与 `tiny_parse`

**Files:**
- Create: `amo-display/main/tiny_parse.h`, `amo-display/main/tiny_parse.c`
- Create: `amo-display/host_test/Makefile`, `amo-display/host_test/test_tiny_parse.c`, `amo-display/host_test/greatest.h`

**Interfaces:**
- Consumes: 无
- Produces:
  - `typedef struct { int waiting, running, idle; char host[32]; char name[64]; char detail[80]; int cpu, mem, gpu, net_kb; bool ok; } tiny_t;`
  - `bool tiny_parse(const char *body, tiny_t *out);`

这是全项目最该被测的一段：宿主端改一个字节，这里就可能整屏乱掉，而板子不在手边。
所以它写成不含任何 ESP 头文件的纯 C，用主机 gcc 编译成可执行文件直接跑。

- [ ] **Step 1: 放一个极简断言框架**

`amo-display/host_test/greatest.h` —— 不引第三方库，自己写 40 行够用的：

```c
#ifndef GREATEST_H
#define GREATEST_H
#include <stdio.h>
#include <string.h>

static int g_pass = 0, g_fail = 0;

#define CHECK(cond, msg) do { \
    if (cond) { g_pass++; } \
    else { g_fail++; printf("  FAIL %s:%d  %s\n", __FILE__, __LINE__, msg); } \
} while (0)

#define CHECK_STR(a, b) do { \
    if (strcmp((a), (b)) == 0) { g_pass++; } \
    else { g_fail++; printf("  FAIL %s:%d  \"%s\" != \"%s\"\n", __FILE__, __LINE__, (a), (b)); } \
} while (0)

#define CHECK_INT(a, b) do { \
    if ((a) == (b)) { g_pass++; } \
    else { g_fail++; printf("  FAIL %s:%d  %d != %d\n", __FILE__, __LINE__, (int)(a), (int)(b)); } \
} while (0)

#define REPORT() do { \
    printf("%d passed, %d failed\n", g_pass, g_fail); \
    return g_fail ? 1 : 0; \
} while (0)

#endif
```

- [ ] **Step 2: 写失败的测试**

`amo-display/host_test/test_tiny_parse.c`：

```c
#include "greatest.h"
#include "../main/tiny_parse.h"

static void test_full_payload(void) {
    tiny_t t;
    const char *body = "1|3,1,2|WORKSTATION\n恐龙公园初始化\tBash\n12,41,3,8";
    CHECK(tiny_parse(body, &t), "应解析成功");
    CHECK_INT(t.waiting, 3);
    CHECK_INT(t.running, 1);
    CHECK_INT(t.idle, 2);
    CHECK_STR(t.host, "WORKSTATION");
    CHECK_STR(t.name, "恐龙公园初始化");
    CHECK_STR(t.detail, "Bash");
    CHECK_INT(t.cpu, 12);
    CHECK_INT(t.mem, 41);
    CHECK_INT(t.gpu, 3);
    CHECK_INT(t.net_kb, 8);
}

static void test_no_sessions_second_line_empty(void) {
    tiny_t t;
    const char *body = "1|0,0,0|HOSTA\n\n5,6,-1,0";
    CHECK(tiny_parse(body, &t), "空第二行也应解析成功");
    CHECK_INT(t.waiting, 0);
    CHECK_STR(t.name, "");
    CHECK_STR(t.detail, "");
    CHECK_INT(t.gpu, -1);
}

static void test_absent_gpu_is_minus_one(void) {
    tiny_t t;
    tiny_parse("1|0,0,1|H\nx\ty\n1,2,-1,3", &t);
    CHECK_INT(t.gpu, -1);
}

static void test_name_without_detail(void) {
    tiny_t t;
    CHECK(tiny_parse("1|1,0,0|H\n只有名字\n1,2,3,4", &t), "没有 tab 也要能解析");
    CHECK_STR(t.name, "只有名字");
    CHECK_STR(t.detail, "");
}

static void test_rejects_wrong_version(void) {
    tiny_t t;
    CHECK(!tiny_parse("2|1,0,0|H\nx\ty\n1,2,3,4", &t), "版本号不认识就该拒绝");
}

static void test_rejects_truncated_body(void) {
    tiny_t t;
    CHECK(!tiny_parse("1|1,0,0|H\nx\ty", &t), "少一行就该拒绝");
    CHECK(!tiny_parse("", &t), "空 body 就该拒绝");
    CHECK(!tiny_parse("garbage", &t), "垃圾数据就该拒绝");
}

static void test_long_fields_are_truncated_not_overflowed(void) {
    /* 服务端 detail 上限 60 字符，但固件不能信任对端 */
    char body[1024];
    snprintf(body, sizeof(body), "1|1,0,0|%s\n%s\t%s\n1,2,3,4",
             "H23456789012345678901234567890123456789012345678901234567890",
             "N23456789012345678901234567890123456789012345678901234567890123456789012345678901234567890",
             "D23456789012345678901234567890123456789012345678901234567890123456789012345678901234567890");
    tiny_t t;
    CHECK(tiny_parse(body, &t), "超长字段应截断而不是拒绝");
    CHECK(strlen(t.host) < sizeof(t.host), "host 不得溢出");
    CHECK(strlen(t.name) < sizeof(t.name), "name 不得溢出");
    CHECK(strlen(t.detail) < sizeof(t.detail), "detail 不得溢出");
}

static void test_no_cwd_ever_appears(void) {
    /* 宿主端保证不下发 cwd；这里断言解析器也不会凭空造出路径字段 */
    tiny_t t;
    tiny_parse("1|1,0,0|H\nAmo\tBash\n1,2,3,4", &t);
    CHECK(strstr(t.detail, ":\\") == NULL, "detail 里不该有盘符路径");
}

int main(void) {
    printf("tiny_parse:\n");
    test_full_payload();
    test_no_sessions_second_line_empty();
    test_absent_gpu_is_minus_one();
    test_name_without_detail();
    test_rejects_wrong_version();
    test_rejects_truncated_body();
    test_long_fields_are_truncated_not_overflowed();
    test_no_cwd_ever_appears();
    REPORT();
}
```

`amo-display/host_test/Makefile`：

```makefile
CC ?= gcc
CFLAGS = -std=c11 -Wall -Wextra -Werror -g

all: test_tiny_parse
	./test_tiny_parse

test_tiny_parse: test_tiny_parse.c ../main/tiny_parse.c
	$(CC) $(CFLAGS) -o $@ $^

clean:
	rm -f test_tiny_parse test_hosts *.exe
```

- [ ] **Step 3: 跑测试确认失败**

```powershell
cd amo-display\host_test
make
```

Expected: 编译失败，`tiny_parse.h: No such file or directory`

若 `make` 或 `gcc` 不存在：IDF 工具链里带的是交叉编译器，不能编主机程序。
装 MSYS2/MinGW 或用 `winget install GnuWin32.Make`。这是必需的——
没有主机测试台，这块固件在板子接上之前就完全无法验证。

- [ ] **Step 4: 写实现**

`amo-display/main/tiny_parse.h`：

```c
#pragma once
#include <stdbool.h>

#define TINY_VERSION 1

typedef struct {
    int  waiting, running, idle;
    char host[32];
    char name[64];
    char detail[80];
    int  cpu, mem, gpu, net_kb;
} tiny_t;

/* 解析 /api/tiny 的三行响应。失败返回 false，此时 *out 内容未定义。
   不信任对端：任何字段超长都截断，不溢出。 */
bool tiny_parse(const char *body, tiny_t *out);
```

`amo-display/main/tiny_parse.c`：

```c
#include "tiny_parse.h"
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

/* 从 src 拷贝到 dst，最多 cap-1 字节，保证 NUL 结尾。 */
static void copy_clamped(char *dst, size_t cap, const char *src, size_t len)
{
    if (len >= cap) {
        len = cap - 1;
    }
    memcpy(dst, src, len);
    dst[len] = '\0';
}

/* 返回本行结束位置（指向 '\n' 或字符串结尾）。 */
static const char *line_end(const char *p)
{
    while (*p && *p != '\n') {
        p++;
    }
    return p;
}

bool tiny_parse(const char *body, tiny_t *out)
{
    if (!body || !out) {
        return false;
    }

    /* ---- 第 1 行：版本|w,r,i|主机名 ---- */
    const char *l1 = body;
    const char *l1_end = line_end(l1);
    if (*l1_end != '\n') {
        return false;                       /* 只有一行，不合法 */
    }

    int ver = 0, w = 0, r = 0, i = 0;
    int consumed = 0;
    if (sscanf(l1, "%d|%d,%d,%d|%n", &ver, &w, &r, &i, &consumed) != 4 || consumed == 0) {
        return false;
    }
    if (ver != TINY_VERSION) {
        return false;                       /* 版本不认识就整体拒绝，别猜 */
    }
    const char *host = l1 + consumed;
    if (host > l1_end) {
        return false;
    }
    copy_clamped(out->host, sizeof(out->host), host, (size_t)(l1_end - host));
    out->waiting = w;
    out->running = r;
    out->idle = i;

    /* ---- 第 2 行：会话名\tdetail（可为空行）---- */
    const char *l2 = l1_end + 1;
    const char *l2_end = line_end(l2);
    if (*l2_end != '\n') {
        return false;                       /* 缺第三行 */
    }
    const char *tab = memchr(l2, '\t', (size_t)(l2_end - l2));
    if (tab) {
        copy_clamped(out->name, sizeof(out->name), l2, (size_t)(tab - l2));
        copy_clamped(out->detail, sizeof(out->detail), tab + 1, (size_t)(l2_end - tab - 1));
    } else {
        copy_clamped(out->name, sizeof(out->name), l2, (size_t)(l2_end - l2));
        out->detail[0] = '\0';
    }

    /* ---- 第 3 行：cpu,mem,gpu,net ---- */
    const char *l3 = l2_end + 1;
    if (sscanf(l3, "%d,%d,%d,%d", &out->cpu, &out->mem, &out->gpu, &out->net_kb) != 4) {
        return false;
    }

    return true;
}
```

- [ ] **Step 5: 跑测试确认通过**

```powershell
cd amo-display\host_test
make
```

Expected: `24 passed, 0 failed`（断言条数以实际为准，failed 必须为 0）

- [ ] **Step 6: Commit**

```bash
git add amo-display/main/tiny_parse.h amo-display/main/tiny_parse.c amo-display/host_test/
git commit -m "feat: /api/tiny 三行解析，纯 C 可在主机上单测"
```

---

### Task 2: 已配对主机表

**Files:**
- Create: `amo-display/main/hosts.h`, `amo-display/main/hosts.c`
- Create: `amo-display/host_test/test_hosts.c`
- Modify: `amo-display/host_test/Makefile`

**Interfaces:**
- Consumes: 无
- Produces:
  - `#define HOSTS_MAX 4`
  - `typedef struct { char host_id[16]; char name[32]; char ip[16]; int port; char token[40]; bool online; } host_t;`
  - `typedef struct { host_t items[HOSTS_MAX]; int count; int current; } hosts_t;`
  - `void hosts_init(hosts_t *h);`
  - `int hosts_find(const hosts_t *h, const char *host_id);` —— 返回下标，找不到返回 -1
  - `bool hosts_upsert(hosts_t *h, const host_t *item);` —— 按 host_id 去重；满了返回 false
  - `void hosts_set_addr(hosts_t *h, const char *host_id, const char *ip, int port);`
  - `void hosts_cycle(hosts_t *h);` —— 短按切换到下一台
  - `const host_t *hosts_current(const hosts_t *h);` —— 无主机时返回 NULL

按 `host_id` 而不是 IP 去重，是因为 IP 会变（DHCP 续约、切网线），而 `host_id` 是宿主端
`uuid4().hex[:8]` 持久化在配置文件里的。设备重新发现同一台主机时必须认出它、只更新地址。

- [ ] **Step 1: 写失败的测试**

`amo-display/host_test/test_hosts.c`：

```c
#include "greatest.h"
#include "../main/hosts.h"

static host_t mk(const char *id, const char *name, const char *ip)
{
    host_t h = {0};
    snprintf(h.host_id, sizeof(h.host_id), "%s", id);
    snprintf(h.name, sizeof(h.name), "%s", name);
    snprintf(h.ip, sizeof(h.ip), "%s", ip);
    snprintf(h.token, sizeof(h.token), "tok-%s", id);
    h.port = 8787;
    return h;
}

static void test_starts_empty(void) {
    hosts_t h; hosts_init(&h);
    CHECK_INT(h.count, 0);
    CHECK(hosts_current(&h) == NULL, "空表 current 应为 NULL");
}

static void test_upsert_adds(void) {
    hosts_t h; hosts_init(&h);
    host_t a = mk("a1b2c3d4", "WORKSTATION", "192.168.1.100");
    CHECK(hosts_upsert(&h, &a), "首次插入应成功");
    CHECK_INT(h.count, 1);
    CHECK_STR(hosts_current(&h)->name, "WORKSTATION");
}

static void test_upsert_dedupes_by_host_id(void) {
    hosts_t h; hosts_init(&h);
    host_t a = mk("a1b2c3d4", "WORKSTATION", "192.168.1.100");
    hosts_upsert(&h, &a);
    host_t again = mk("a1b2c3d4", "WORKSTATION", "192.168.1.177");   /* IP 变了 */
    CHECK(hosts_upsert(&h, &again), "重复 host_id 应更新而不是拒绝");
    CHECK_INT(h.count, 1);
    CHECK_STR(h.items[0].ip, "192.168.1.177");
}

static void test_set_addr_only_touches_address(void) {
    hosts_t h; hosts_init(&h);
    host_t a = mk("a1b2c3d4", "WORKSTATION", "192.168.1.100");
    hosts_upsert(&h, &a);
    hosts_set_addr(&h, "a1b2c3d4", "10.0.0.9", 9999);
    CHECK_STR(h.items[0].ip, "10.0.0.9");
    CHECK_INT(h.items[0].port, 9999);
    CHECK_STR(h.items[0].token, "tok-a1b2c3d4");   /* 令牌不能被地址更新冲掉 */
}

static void test_set_addr_ignores_unknown_host(void) {
    hosts_t h; hosts_init(&h);
    hosts_set_addr(&h, "nobody", "10.0.0.9", 1);   /* 不得崩溃 */
    CHECK_INT(h.count, 0);
}

static void test_full_table_rejects(void) {
    hosts_t h; hosts_init(&h);
    for (int i = 0; i < HOSTS_MAX; i++) {
        char id[16];
        snprintf(id, sizeof(id), "id%d", i);
        host_t x = mk(id, "H", "1.2.3.4");
        CHECK(hosts_upsert(&h, &x), "未满时应成功");
    }
    host_t extra = mk("overflow", "H", "1.2.3.4");
    CHECK(!hosts_upsert(&h, &extra), "满了应返回 false 而不是覆盖");
    CHECK_INT(h.count, HOSTS_MAX);
}

static void test_cycle_wraps(void) {
    hosts_t h; hosts_init(&h);
    host_t a = mk("id0", "A", "1.1.1.1");
    host_t b = mk("id1", "B", "2.2.2.2");
    hosts_upsert(&h, &a);
    hosts_upsert(&h, &b);
    CHECK_STR(hosts_current(&h)->name, "A");
    hosts_cycle(&h);
    CHECK_STR(hosts_current(&h)->name, "B");
    hosts_cycle(&h);
    CHECK_STR(hosts_current(&h)->name, "A");
}

static void test_cycle_on_empty_does_not_crash(void) {
    hosts_t h; hosts_init(&h);
    hosts_cycle(&h);
    CHECK(hosts_current(&h) == NULL, "空表 cycle 后仍应为 NULL");
}

int main(void) {
    printf("hosts:\n");
    test_starts_empty();
    test_upsert_adds();
    test_upsert_dedupes_by_host_id();
    test_set_addr_only_touches_address();
    test_set_addr_ignores_unknown_host();
    test_full_table_rejects();
    test_cycle_wraps();
    test_cycle_on_empty_does_not_crash();
    REPORT();
}
```

把 `Makefile` 改成同时构建两个测试：

```makefile
CC ?= gcc
CFLAGS = -std=c11 -Wall -Wextra -Werror -g

all: test_tiny_parse test_hosts
	./test_tiny_parse
	./test_hosts

test_tiny_parse: test_tiny_parse.c ../main/tiny_parse.c
	$(CC) $(CFLAGS) -o $@ $^

test_hosts: test_hosts.c ../main/hosts.c
	$(CC) $(CFLAGS) -o $@ $^

clean:
	rm -f test_tiny_parse test_hosts *.exe
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd amo-display\host_test && make`
Expected: `hosts.h: No such file or directory`

- [ ] **Step 3: 写实现**

`amo-display/main/hosts.h`：

```c
#pragma once
#include <stdbool.h>

#define HOSTS_MAX 4

typedef struct {
    char host_id[16];      /* 宿主端 uuid4().hex[:8]，IP 变了也认得出是同一台 */
    char name[32];
    char ip[16];
    int  port;
    char token[40];        /* 32 位十六进制 + 余量 */
    bool online;
} host_t;

typedef struct {
    host_t items[HOSTS_MAX];
    int    count;
    int    current;        /* 当前盯着哪一台 */
} hosts_t;

void hosts_init(hosts_t *h);
int  hosts_find(const hosts_t *h, const char *host_id);
bool hosts_upsert(hosts_t *h, const host_t *item);
void hosts_set_addr(hosts_t *h, const char *host_id, const char *ip, int port);
void hosts_cycle(hosts_t *h);
const host_t *hosts_current(const hosts_t *h);
```

`amo-display/main/hosts.c`：

```c
#include "hosts.h"
#include <string.h>
#include <stdio.h>

void hosts_init(hosts_t *h)
{
    memset(h, 0, sizeof(*h));
}

int hosts_find(const hosts_t *h, const char *host_id)
{
    for (int i = 0; i < h->count; i++) {
        if (strcmp(h->items[i].host_id, host_id) == 0) {
            return i;
        }
    }
    return -1;
}

bool hosts_upsert(hosts_t *h, const host_t *item)
{
    int at = hosts_find(h, item->host_id);
    if (at >= 0) {
        h->items[at] = *item;          /* 同一台，整条更新 */
        return true;
    }
    if (h->count >= HOSTS_MAX) {
        return false;                  /* 满了就拒绝，不静默覆盖别人 */
    }
    h->items[h->count++] = *item;
    return true;
}

void hosts_set_addr(hosts_t *h, const char *host_id, const char *ip, int port)
{
    int at = hosts_find(h, host_id);
    if (at < 0) {
        return;                        /* 没配对过的主机，地址不记 */
    }
    snprintf(h->items[at].ip, sizeof(h->items[at].ip), "%s", ip);
    h->items[at].port = port;
}

void hosts_cycle(hosts_t *h)
{
    if (h->count <= 0) {
        return;
    }
    h->current = (h->current + 1) % h->count;
}

const host_t *hosts_current(const hosts_t *h)
{
    if (h->count <= 0) {
        return NULL;
    }
    return &h->items[h->current];
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd amo-display\host_test && make`
Expected: 两个测试都 `0 failed`

- [ ] **Step 5: Commit**

```bash
git add amo-display/main/hosts.h amo-display/main/hosts.c amo-display/host_test/
git commit -m "feat: 已配对主机表，按 host_id 去重使 IP 变化不影响识别"
```

---

### Task 3: 工程骨架与板级组件

**Files:**
- Create: `amo-display/CMakeLists.txt`, `amo-display/sdkconfig.defaults`, `amo-display/partitions.csv`, `amo-display/Kconfig.projbuild`
- Create: `amo-display/main/CMakeLists.txt`, `amo-display/main/main.c`
- Create: `amo-display/components/lilygo_board/` （裁自官方）

**Interfaces:**
- Consumes: Task 0 验证过的构建环境
- Produces: 一个能 `idf.py build` 通过、烧进去能点亮屏幕的最小工程

- [ ] **Step 1: 裁剪板级代码**

从 Task 0 克隆的 `D:\Lvwenchao\geek\esp-probe\idf-ref` 拷贝，只取本板用得到的：

```powershell
$src = "D:\Lvwenchao\geek\esp-probe\idf-ref"
$dst = "D:\Lvwenchao\geek\Amo\amo-display\components\lilygo_board"
New-Item -ItemType Directory -Force -Path $dst | Out-Null
foreach ($f in @("main\amoled_driver.c","main\amoled_driver.h","main\initSequence.c",
                 "main\initSequence.h","main\i2c_driver.c","main\i2c_driver.h",
                 "main\power_driver.cpp","main\power_driver.h","main\product_pins.h",
                 "main\tft_driver.h")) {
  Copy-Item (Join-Path $src $f) $dst
}
Copy-Item (Join-Path $src "components\XPowersLib") (Join-Path $dst "..\XPowersLib") -Recurse
Copy-Item (Join-Path $src "components\SensorLib") (Join-Path $dst "..\SensorLib") -Recurse
```

**不拷** `display_*.c`（那是 16 块板的分支逻辑）和 `touch_driver.cpp`（本轮不用触摸）。
`product_pins.h` 里除 `CONFIG_LILYGO_T_AMOLED_LITE_147` 以外的分支可以留着，
它们靠 `#if` 编译期裁掉，删了反而增加与上游对比的难度。

`amo-display/components/lilygo_board/CMakeLists.txt`：

```cmake
idf_component_register(
    SRCS "amoled_driver.c" "initSequence.c" "i2c_driver.c" "power_driver.cpp"
    INCLUDE_DIRS "."
    REQUIRES driver esp_lcd
    PRIV_REQUIRES XPowersLib
)
```

- [ ] **Step 2: 工程配置**

`amo-display/CMakeLists.txt`：

```cmake
cmake_minimum_required(VERSION 3.16)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(amo_display)
```

`amo-display/partitions.csv` —— 字库要占 flash，默认 1MB app 分区不够：

```csv
# Name,   Type, SubType, Offset,   Size,    Flags
nvs,      data, nvs,     0x9000,   0x6000,
phy_init, data, phy,     0xf000,   0x1000,
factory,  app,  factory, 0x10000,  3M,
```

`amo-display/sdkconfig.defaults` —— 以官方 t-amoled-lite 为底，加上本项目要的：

```
CONFIG_LILYGO_T_AMOLED_LITE_147=y
CONFIG_IDF_TARGET="esp32s3"
CONFIG_IDF_TARGET_ESP32S3=y
CONFIG_SPIRAM=y
CONFIG_SPIRAM_MODE_OCT=y
CONFIG_SPIRAM_TYPE_AUTO=y
CONFIG_ESP_CONSOLE_USB_SERIAL_JTAG=y
CONFIG_ESP_DEFAULT_CPU_FREQ_MHZ_240=y
CONFIG_ESPTOOLPY_FLASHSIZE_16MB=y
CONFIG_PARTITION_TABLE_CUSTOM=y
CONFIG_PARTITION_TABLE_CUSTOM_FILENAME="partitions.csv"
CONFIG_LV_COLOR_16_SWAP=y
CONFIG_LV_COLOR_DEPTH_16=y
CONFIG_LV_MEM_SIZE_KILOBYTES=48
CONFIG_LV_FONT_MONTSERRAT_14=y
CONFIG_LV_FONT_MONTSERRAT_28=y
CONFIG_LV_FONT_MONTSERRAT_48=y
CONFIG_MDNS_MAX_SERVICES=10
```

`amo-display/Kconfig.projbuild`：

```
menu "Agent Display"

config AGENT_WIFI_SSID
    string "WiFi SSID"
    default "your-ssid"

config AGENT_WIFI_PASSWORD
    string "WiFi 密码"
    default "your-pass"

config AGENT_BUZZER_GPIO
    int "外接无源蜂鸣器 GPIO（-1 关闭）"
    default -1
    help
        本板没有板载蜂鸣器。要加就接一个空闲脚，建议 GPIO17。
        绝对不要用 GPIO2 —— 那是本板的 I2C SCL，会打死 PMU 和触摸。

config AGENT_TINY_FULL_DETAIL
    bool "请求完整命令参数（?d=full）"
    default n
    help
        默认只取工具名。桌面小屏是公开展示的，别人走过就能看见。

endmenu
```

`amo-display/main/CMakeLists.txt` —— **只声明此刻真实存在的东西**。
`agent_ui` 组件到 Task 9 才创建，现在就写进 `REQUIRES` 会让 CMake 直接报组件不存在：

```cmake
idf_component_register(
    SRCS "main.c" "tiny_parse.c" "hosts.c"
    INCLUDE_DIRS "."
    REQUIRES lilygo_board
)
```

后续任务会按需扩充：Task 6 加 `nvs_flash esp_wifi mdns`，Task 7 加 `esp_http_client`，
Task 9 加 `lvgl agent_ui`。每次扩充后都要 `idf.py reconfigure`。

`amo-display/main/idf_component.yml`：

```yaml
dependencies:
  lvgl/lvgl: "^8.3.11"
  espressif/mdns: "^1.2.0"
  idf:
    version: ">=5.2.0"
```

- [ ] **Step 3: 最小 main —— 点亮屏幕并刷一块纯色**

`amo-display/main/main.c`：

```c
#include <stdio.h>
#include "freertos/FreeRTOS.h"
#include "freertos/task.h"
#include "esp_log.h"
#include "amoled_driver.h"

static const char *TAG = "amo";

void app_main(void)
{
    ESP_LOGI(TAG, "启动");
    ESP_ERROR_CHECK(amoled_driver_init());
    amoled_set_brightness(60);
    ESP_LOGI(TAG, "屏幕已初始化，分辨率 %dx%d", AMOLED_WIDTH, AMOLED_HEIGHT);

    while (1) {
        vTaskDelay(pdMS_TO_TICKS(1000));
    }
}
```

实际函数名以 `amoled_driver.h` 为准。若签名不同，**按头文件改这里，不要改驱动** ——
板级代码是从上游裁来的，改动越少越容易跟上游对比。

- [ ] **Step 4: 编译**

```powershell
cd D:\Lvwenchao\geek\Amo\amo-display
$env:IDF_PATH       = "C:\Users\CC\esp\v5.2\esp-idf"
$env:IDF_TOOLS_PATH = "D:\Lvwenchao\geek\esp\Espressif\frameworks\esp-idf-v5.2.1\tools"
& "$env:IDF_PATH\export.ps1"
idf.py set-target esp32s3
idf.py reconfigure
idf.py build
```

Expected: `Project build complete.`，并打印 app 分区占用百分比。

- [ ] **Step 5: 记录烧录命令，不执行**

板子当前未连接（串口只有主板 COM1）。把命令写进 `amo-display/README.md`：

```markdown
## 烧录

```powershell
idf.py -p COM<n> flash monitor
```

板子用 USB-Serial-JTAG，插上后会多出一个 COM 口。`Ctrl+]` 退出监视器。
```

- [ ] **Step 6: Commit**

```bash
git add amo-display/
git commit -m "feat: ESP32 工程骨架与板级组件，最小 main 可点亮屏幕"
```

---

### Task 4: 中文字库

**Files:**
- Create: `amo-display/tools/gen_fonts.ps1`
- Create: `amo-display/components/agent_ui/fonts/font_status_28.c`
- Create: `amo-display/components/agent_ui/fonts/font_cjk_16.c`
- Create: `amo-display/components/agent_ui/ui_strings.h`

**Interfaces:**
- Consumes: 无
- Produces: `lv_font_status_28`、`lv_font_cjk_16` 两个 LVGL 字体符号；`UI_S_*` 文案宏

字库产物**签入仓库**，这样构建机不需要 node。`gen_fonts.ps1` 只在改字号或加字时才跑。

- [ ] **Step 1: 定义文案，先确定要哪些字**

`amo-display/components/agent_ui/ui_strings.h`：

```c
#pragma once

/* 与宿主端 phrases.py 的 STATE_LABEL_SHORT 逐字一致。
   改这里必须同步改 phrases.py，tests/test_firmware_copy.py 会挡住漂移。 */
#define UI_S_WAITING  "该你了"
#define UI_S_RUNNING  "干着呢"
#define UI_S_IDLE     "摸鱼中"
#define UI_S_STALE    "没声儿了"

/* 连不上任何主机 —— 与"某个会话失联"是两回事，不得合并 */
#define UI_S_NOLINK   "连不上"

#define UI_S_PAIRING  "配对中"
#define UI_S_PAIR_OK  "配上了"
#define UI_S_PAIR_NO  "没找着"
#define UI_S_NOHOST   "还没配对"
```

- [ ] **Step 2: 写生成脚本**

`amo-display/tools/gen_fonts.ps1`：

```powershell
# 生成 LVGL 中文字库。改字号或加字后重跑，产物签入仓库，构建机无需 node。
#
# 底字用思源黑体（开源），不用系统微软雅黑 —— 嵌进固件分发时没有授权顾虑。
$ErrorActionPreference = "Stop"

$fontDir = Join-Path $PSScriptRoot "..\components\agent_ui\fonts"
$ttf = Join-Path $PSScriptRoot "SourceHanSansSC-Regular.otf"

if (-not (Test-Path $ttf)) {
    Write-Host "请先下载思源黑体到 $ttf"
    Write-Host "https://github.com/adobe-fonts/source-han-sans/releases"
    exit 1
}

# 状态词用到的字：只有十几个，大字号也很小
$statusChars = "该你了干着呢摸鱼中没声儿连不上配对了找着还"

# 会话名可能是任意中文，打 GB2312 一级+二级常用字
# lv_font_conv 的 --range 用 Unicode 码点区间
$cjkRange = "0x4E00-0x9FA5"
$asciiRange = "0x20-0x7F"

Write-Host "生成 font_status_28（状态词专用，小字符集大字号）..."
npx lv_font_conv --font $ttf --size 28 --bpp 4 --format lvgl `
    --symbols $statusChars `
    --lv-font-name lv_font_status_28 `
    -o (Join-Path $fontDir "font_status_28.c")

Write-Host "生成 font_cjk_16（会话名与 detail）..."
npx lv_font_conv --font $ttf --size 16 --bpp 4 --format lvgl `
    --range "$asciiRange" --range "$cjkRange" `
    --lv-font-name lv_font_cjk_16 `
    -o (Join-Path $fontDir "font_cjk_16.c")

Write-Host "完成。产物已写入 $fontDir，记得一并提交。"
```

- [ ] **Step 3: 装 lv_font_conv 并生成**

```powershell
node --version      # 本机实测 v22.12.0，可用
cd amo-display\tools
# 下载思源黑体到本目录（脚本会提示 URL）
.\gen_fonts.ps1
```

Expected: 两个 `.c` 文件生成。`font_cjk_16.c` 会比较大（20000+ 字符 4bpp，
预计 2-4MB），**若超过 app 分区 3M 的余量，把 `--range` 换成
`--symbols` 加一份常用 3500 字表**，体积会降到 300KB 量级。

- [ ] **Step 4: 确认体积可接受**

```powershell
Get-ChildItem ..\components\agent_ui\fonts\*.c | Select-Object Name, @{n='MB';e={[math]::Round($_.Length/1MB,2)}}
```

Expected: 两个文件加起来留有余量（app 分区 3M，代码本身约 1M）。
超了就回 Step 3 缩小字符集——**不要改大分区表**，16MB flash 虽然放得下，
但一个桌面状态显示器不需要为了字库把 app 分区撑到 8M。

- [ ] **Step 5: Commit**

```bash
git add amo-display/tools/gen_fonts.ps1 amo-display/components/agent_ui/fonts/ amo-display/components/agent_ui/ui_strings.h
git commit -m "feat: 中文字库生成脚本与产物，状态词与会话名分两份"
```

---

### Task 5: 文案防漂移测试（宿主端）

**Files:**
- Create: `tests/test_firmware_copy.py`

**Interfaces:**
- Consumes: `phrases.py`、`amo-display/components/agent_ui/ui_strings.h`
- Produces: 无

固件是 C，导不了 `phrases.py`——和浏览器同一个问题，同一个解法。
spec §6 要求三端共用一份文案，而这是唯一能真正约束固件那一份的东西。

- [ ] **Step 1: 写失败的测试**

`tests/test_firmware_copy.py`：

```python
"""固件是 C，导不了 phrases.py，只能靠这条测试挡住三端文案漂移。

与 tests/test_page_copy.py 同一个思路：页面和固件各自硬编码一份中文，
这两条测试是唯一保证它们和 phrases.py 一致的东西。
"""
import re
from pathlib import Path

import pytest

import phrases

HEADER = Path(__file__).resolve().parent.parent / "amo-display" / "components" / "agent_ui" / "ui_strings.h"

MAPPING = {
    "UI_S_WAITING": "waiting",
    "UI_S_RUNNING": "running",
    "UI_S_IDLE": "idle",
    "UI_S_STALE": "stale",
}


def _defines(text: str) -> dict:
    return dict(re.findall(r'#define\s+(UI_S_\w+)\s+"([^"]*)"', text))


@pytest.mark.skipif(not HEADER.exists(), reason="ESP32 工程尚未创建")
def test_firmware_state_words_match_phrases():
    d = _defines(HEADER.read_text(encoding="utf-8"))
    for macro, state in MAPPING.items():
        assert macro in d, f"ui_strings.h 缺少 {macro}"
        assert d[macro] == phrases.STATE_LABEL_SHORT[state], (
            f"{macro} 与 phrases.STATE_LABEL_SHORT[{state!r}] 不一致"
        )


@pytest.mark.skipif(not HEADER.exists(), reason="ESP32 工程尚未创建")
def test_nolink_is_distinct_from_stale():
    """连不上任何主机 与 某个会话失联 是两回事，合并会掩盖故障。"""
    d = _defines(HEADER.read_text(encoding="utf-8"))
    assert d["UI_S_NOLINK"] != d["UI_S_STALE"]


@pytest.mark.skipif(not HEADER.exists(), reason="ESP32 工程尚未创建")
def test_all_firmware_strings_are_chinese():
    d = _defines(HEADER.read_text(encoding="utf-8"))
    for macro, value in d.items():
        assert not value.isascii(), f"{macro} 应为中文口语: {value!r}"


@pytest.mark.skipif(not HEADER.exists(), reason="ESP32 工程尚未创建")
def test_status_words_fit_the_narrow_screen():
    """状态词用 28px 大字号显示，368px 宽度下最多容纳 4 个字。"""
    d = _defines(HEADER.read_text(encoding="utf-8"))
    for macro in list(MAPPING) + ["UI_S_NOLINK"]:
        assert len(d[macro]) <= 4, f"{macro} 超过 4 个字: {d[macro]}"
```

- [ ] **Step 2: 跑测试**

Run: `python -m pytest tests/test_firmware_copy.py -v`
Expected: Task 4 完成后全部 PASS；未完成时全部 skip 且不报错。

- [ ] **Step 3: 故意改坏，确认测试能抓住**

把 `ui_strings.h` 里 `UI_S_WAITING` 临时改成 `"该你啦"`，重跑：

Run: `python -m pytest tests/test_firmware_copy.py -v`
Expected: FAIL，提示与 `phrases.STATE_LABEL_SHORT['waiting']` 不一致。

改回来，再跑一次确认恢复 PASS。**这一步不能省** ——
一条永远为真的防漂移测试比没有测试更坏，它会让人以为有东西在看着。

- [ ] **Step 4: Commit**

```bash
git add tests/test_firmware_copy.py
git commit -m "test: 固件文案防漂移，挡住 ui_strings.h 与 phrases.py 分叉"
```

---

### Task 6: WiFi 与 mDNS 发现

**Files:**
- Create: `amo-display/main/net_wifi.h`, `amo-display/main/net_wifi.c`
- Create: `amo-display/main/discovery.h`, `amo-display/main/discovery.c`
- Modify: `amo-display/main/CMakeLists.txt`, `amo-display/main/main.c`

**Interfaces:**
- Consumes: `hosts.h` 的 `host_t`
- Produces:
  - `esp_err_t net_wifi_start(void);` —— 阻塞直到拿到 IP 或超时
  - `bool net_wifi_is_up(void);`
  - `int discovery_scan(host_t *found, int max, int timeout_ms);` —— 返回发现数量，只填 `host_id`/`name`/`ip`/`port`

- [ ] **Step 1: WiFi**

`amo-display/main/net_wifi.h`：

```c
#pragma once
#include "esp_err.h"
#include <stdbool.h>

esp_err_t net_wifi_start(void);
bool      net_wifi_is_up(void);
```

`amo-display/main/net_wifi.c`：

```c
#include "net_wifi.h"
#include <string.h>
#include "freertos/FreeRTOS.h"
#include "freertos/event_groups.h"
#include "esp_wifi.h"
#include "esp_event.h"
#include "esp_log.h"
#include "nvs_flash.h"

static const char *TAG = "wifi";
static EventGroupHandle_t s_events;
static bool s_up = false;

#define GOT_IP   BIT0
#define FAILED   BIT1

static void on_event(void *arg, esp_event_base_t base, int32_t id, void *data)
{
    (void)arg; (void)data;
    if (base == WIFI_EVENT && id == WIFI_EVENT_STA_START) {
        esp_wifi_connect();
    } else if (base == WIFI_EVENT && id == WIFI_EVENT_STA_DISCONNECTED) {
        s_up = false;
        ESP_LOGW(TAG, "断开，重连");
        esp_wifi_connect();                 /* 一直重连：这是常驻设备，不该放弃 */
    } else if (base == IP_EVENT && id == IP_EVENT_STA_GOT_IP) {
        s_up = true;
        xEventGroupSetBits(s_events, GOT_IP);
    }
}

bool net_wifi_is_up(void) { return s_up; }

esp_err_t net_wifi_start(void)
{
    esp_err_t err = nvs_flash_init();
    if (err == ESP_ERR_NVS_NO_FREE_PAGES || err == ESP_ERR_NVS_NEW_VERSION_FOUND) {
        ESP_ERROR_CHECK(nvs_flash_erase());
        err = nvs_flash_init();
    }
    ESP_ERROR_CHECK(err);

    s_events = xEventGroupCreate();
    ESP_ERROR_CHECK(esp_netif_init());
    ESP_ERROR_CHECK(esp_event_loop_create_default());
    esp_netif_create_default_wifi_sta();

    wifi_init_config_t cfg = WIFI_INIT_CONFIG_DEFAULT();
    ESP_ERROR_CHECK(esp_wifi_init(&cfg));
    ESP_ERROR_CHECK(esp_event_handler_instance_register(WIFI_EVENT, ESP_EVENT_ANY_ID, &on_event, NULL, NULL));
    ESP_ERROR_CHECK(esp_event_handler_instance_register(IP_EVENT, IP_EVENT_STA_GOT_IP, &on_event, NULL, NULL));

    wifi_config_t wc = {0};
    snprintf((char *)wc.sta.ssid, sizeof(wc.sta.ssid), "%s", CONFIG_AGENT_WIFI_SSID);
    snprintf((char *)wc.sta.password, sizeof(wc.sta.password), "%s", CONFIG_AGENT_WIFI_PASSWORD);

    ESP_ERROR_CHECK(esp_wifi_set_mode(WIFI_MODE_STA));
    ESP_ERROR_CHECK(esp_wifi_set_config(WIFI_IF_STA, &wc));
    ESP_ERROR_CHECK(esp_wifi_start());

    EventBits_t bits = xEventGroupWaitBits(s_events, GOT_IP | FAILED, pdFALSE, pdFALSE,
                                           pdMS_TO_TICKS(20000));
    return (bits & GOT_IP) ? ESP_OK : ESP_ERR_TIMEOUT;
}
```

- [ ] **Step 2: mDNS 发现**

`amo-display/main/discovery.h`：

```c
#pragma once
#include "hosts.h"

/* 查询 _agentdash._tcp，把发现的主机填进 found（只填 host_id/name/ip/port）。
   返回发现数量。TXT 里只有 v/host/id，没有令牌 —— 令牌要靠配对拿。 */
int discovery_scan(host_t *found, int max, int timeout_ms);
```

`amo-display/main/discovery.c`：

```c
#include "discovery.h"
#include <string.h>
#include <stdio.h>
#include "mdns.h"
#include "esp_log.h"

static const char *TAG = "disco";

static const char *txt_get(const mdns_result_t *r, const char *key)
{
    for (size_t i = 0; i < r->txt_count; i++) {
        if (strcmp(r->txt[i].key, key) == 0) {
            return r->txt_value_len[i] ? r->txt[i].value : "";
        }
    }
    return NULL;
}

int discovery_scan(host_t *found, int max, int timeout_ms)
{
    mdns_result_t *results = NULL;
    esp_err_t err = mdns_query_ptr("_agentdash", "_tcp", timeout_ms, 20, &results);
    if (err != ESP_OK) {
        ESP_LOGW(TAG, "查询失败: %s", esp_err_to_name(err));
        return 0;
    }

    int n = 0;
    for (mdns_result_t *r = results; r && n < max; r = r->next) {
        const char *id = txt_get(r, "id");
        const char *host = txt_get(r, "host");
        if (!id || !*id || !r->addr) {
            continue;                        /* 没有 id 的不是我们的服务 */
        }
        host_t *h = &found[n];
        memset(h, 0, sizeof(*h));
        snprintf(h->host_id, sizeof(h->host_id), "%s", id);
        snprintf(h->name, sizeof(h->name), "%s", host ? host : id);
        snprintf(h->ip, sizeof(h->ip), IPSTR, IP2STR(&r->addr->addr.u_addr.ip4));
        h->port = r->port;
        n++;
    }

    mdns_query_results_free(results);
    ESP_LOGI(TAG, "发现 %d 台", n);
    return n;
}
```

- [ ] **Step 3: 接进 main，编译并烧录验证**

`main.c` 里在屏幕初始化后加：

```c
    ESP_ERROR_CHECK(mdns_init());
    if (net_wifi_start() == ESP_OK) {
        ESP_LOGI(TAG, "WiFi 已连接");
        host_t found[HOSTS_MAX];
        int n = discovery_scan(found, HOSTS_MAX, 3000);
        for (int i = 0; i < n; i++) {
            ESP_LOGI(TAG, "发现 %s (%s) @ %s:%d",
                     found[i].name, found[i].host_id, found[i].ip, found[i].port);
        }
    } else {
        ESP_LOGE(TAG, "WiFi 连不上");
    }
```

把 `net_wifi.c` `discovery.c` 加进 `main/CMakeLists.txt` 的 `SRCS`，
并把 `REQUIRES` 扩充为 `lilygo_board nvs_flash esp_wifi mdns`，然后 `idf.py reconfigure`。

- [ ] **Step 4: 编译**

Run: `idf.py build`
Expected: 编译通过

- [ ] **Step 5: 实机验证（需要用户）**

板子未连接，这一步由用户执行：

1. `idf.py menuconfig` → Agent Display → 填 WiFi SSID 与密码
2. 宿主机上确保 `AgentDashboard.exe` 在跑
3. `idf.py -p COM<n> flash monitor`

Expected 串口输出：

```
I (xxxx) wifi: WiFi 已连接
I (xxxx) disco: 发现 1 台
I (xxxx) amo: 发现 USER-20220210RC (be8b9e8a) @ 192.168.1.100:8787
```

若发现 0 台：宿主端 mDNS 是否在广播（宿主机上跑
`python -c "from zeroconf import Zeroconf, ServiceBrowser; import time; zc=Zeroconf(); ServiceBrowser(zc,'_agentdash._tcp.local.',handlers=[lambda **kw: print(kw)]); time.sleep(3); zc.close()"`
应能看到），以及板子和宿主机是否在同一子网（宿主机有 VirtualBox 虚拟网卡
`192.168.56.1`，真实局域网是 `192.168.1.100`，别搞混）。

- [ ] **Step 6: Commit**

```bash
git add amo-display/main/net_wifi.* amo-display/main/discovery.* amo-display/main/CMakeLists.txt amo-display/main/main.c
git commit -m "feat: WiFi 连接与 mDNS 发现宿主机"
```

---

### Task 7: 配对与 NVS 持久化

**Files:**
- Create: `amo-display/main/pairing.h`, `amo-display/main/pairing.c`
- Create: `amo-display/main/hosts_nvs.h`, `amo-display/main/hosts_nvs.c`
- Create: `amo-display/main/buttons.h`, `amo-display/main/buttons.c`
- Modify: `amo-display/main/CMakeLists.txt`, `amo-display/main/main.c`

**Interfaces:**
- Consumes: `hosts.h`、`discovery.h`
- Produces:
  - `bool pairing_request(const char *ip, int port, char *token_out, size_t cap);`
  - `esp_err_t hosts_nvs_load(hosts_t *h);` / `esp_err_t hosts_nvs_save(const hosts_t *h);`
  - `typedef enum { BTN_NONE, BTN_SHORT, BTN_LONG } btn_event_t;`
  - `void buttons_init(void);` / `btn_event_t buttons_poll(void);`

- [ ] **Step 1: 配对请求**

`amo-display/main/pairing.h`：

```c
#pragma once
#include <stdbool.h>
#include <stddef.h>

/* 向指定主机的 /api/pair 要令牌。只有宿主在托盘点过"配对新设备"、
   窗口还开着时才会返回 200，否则 403。成功时把令牌写进 token_out。 */
bool pairing_request(const char *ip, int port, char *token_out, size_t cap);
```

`amo-display/main/pairing.c`：

```c
#include "pairing.h"
#include <string.h>
#include <stdio.h>
#include "esp_http_client.h"
#include "esp_log.h"

static const char *TAG = "pair";

/* 从 {"token":"xxx",...} 里抠出 token，不引 cJSON —— 只有一个字段要读。 */
static bool extract_token(const char *json, char *out, size_t cap)
{
    const char *k = strstr(json, "\"token\"");
    if (!k) {
        return false;
    }
    const char *q = strchr(k + 7, '"');
    if (!q) {
        return false;
    }
    q++;
    const char *end = strchr(q, '"');
    if (!end || (size_t)(end - q) >= cap) {
        return false;
    }
    memcpy(out, q, (size_t)(end - q));
    out[end - q] = '\0';
    return true;
}

bool pairing_request(const char *ip, int port, char *token_out, size_t cap)
{
    char url[64];
    snprintf(url, sizeof(url), "http://%s:%d/api/pair", ip, port);

    esp_http_client_config_t cfg = {
        .url = url,
        .method = HTTP_METHOD_POST,
        .timeout_ms = 3000,
    };
    esp_http_client_handle_t c = esp_http_client_init(&cfg);
    bool ok = false;

    if (esp_http_client_open(c, 0) == ESP_OK) {
        esp_http_client_fetch_headers(c);
        int status = esp_http_client_get_status_code(c);
        if (status == 200) {
            char buf[256] = {0};
            int n = esp_http_client_read_response(c, buf, sizeof(buf) - 1);
            if (n > 0 && extract_token(buf, token_out, cap)) {
                ok = true;
            }
        } else {
            ESP_LOGW(TAG, "%s 返回 %d（配对窗口没开？）", url, status);
        }
    }
    esp_http_client_cleanup(c);
    return ok;
}
```

- [ ] **Step 2: NVS 持久化**

`amo-display/main/hosts_nvs.h`：

```c
#pragma once
#include "esp_err.h"
#include "hosts.h"

esp_err_t hosts_nvs_load(hosts_t *h);
esp_err_t hosts_nvs_save(const hosts_t *h);
```

`amo-display/main/hosts_nvs.c`：

```c
#include "hosts_nvs.h"
#include <string.h>
#include "nvs_flash.h"
#include "nvs.h"
#include "esp_log.h"

static const char *TAG = "hostsnvs";
#define NS   "agentdash"
#define KEY  "hosts"

esp_err_t hosts_nvs_load(hosts_t *h)
{
    hosts_init(h);

    nvs_handle_t nh;
    esp_err_t err = nvs_open(NS, NVS_READONLY, &nh);
    if (err != ESP_OK) {
        return err;                       /* 首次开机没有这个命名空间，正常 */
    }

    size_t len = sizeof(hosts_t);
    err = nvs_get_blob(nh, KEY, h, &len);
    nvs_close(nh);

    if (err != ESP_OK || len != sizeof(hosts_t)) {
        hosts_init(h);                    /* 长度对不上说明结构变过，宁可清空 */
        return ESP_ERR_INVALID_SIZE;
    }
    if (h->count < 0 || h->count > HOSTS_MAX) {
        hosts_init(h);                    /* 不信任 flash 里的内容 */
        return ESP_ERR_INVALID_STATE;
    }
    if (h->current < 0 || h->current >= h->count) {
        h->current = 0;
    }
    ESP_LOGI(TAG, "载入 %d 台已配对主机", h->count);
    return ESP_OK;
}

esp_err_t hosts_nvs_save(const hosts_t *h)
{
    nvs_handle_t nh;
    esp_err_t err = nvs_open(NS, NVS_READWRITE, &nh);
    if (err != ESP_OK) {
        return err;
    }
    err = nvs_set_blob(nh, KEY, h, sizeof(hosts_t));
    if (err == ESP_OK) {
        err = nvs_commit(nh);
    }
    nvs_close(nh);
    return err;
}
```

- [ ] **Step 3: 按键**

`amo-display/main/buttons.h`：

```c
#pragma once

typedef enum { BTN_NONE, BTN_SHORT, BTN_LONG } btn_event_t;

/* GPIO21。GPIO0 是 BOOT strapping 脚，不碰。 */
void        buttons_init(void);
btn_event_t buttons_poll(void);      /* 每 50ms 调一次 */
```

`amo-display/main/buttons.c`：

```c
#include "buttons.h"
#include "driver/gpio.h"
#include "esp_timer.h"

#define BTN_GPIO      21
#define LONG_PRESS_US (3 * 1000 * 1000)

static int64_t s_down_at = 0;
static bool    s_was_down = false;
static bool    s_long_fired = false;

void buttons_init(void)
{
    gpio_config_t cfg = {
        .pin_bit_mask = 1ULL << BTN_GPIO,
        .mode = GPIO_MODE_INPUT,
        .pull_up_en = GPIO_PULLUP_ENABLE,
        .pull_down_en = GPIO_PULLDOWN_DISABLE,
        .intr_type = GPIO_INTR_DISABLE,
    };
    gpio_config(&cfg);
}

btn_event_t buttons_poll(void)
{
    bool down = (gpio_get_level(BTN_GPIO) == 0);   /* 上拉，按下为低 */
    int64_t now = esp_timer_get_time();

    if (down && !s_was_down) {
        s_down_at = now;
        s_long_fired = false;
        s_was_down = true;
        return BTN_NONE;
    }

    if (down && s_was_down && !s_long_fired && (now - s_down_at) >= LONG_PRESS_US) {
        s_long_fired = true;                       /* 长按在到时的那一刻就报，不等松手 */
        return BTN_LONG;
    }

    if (!down && s_was_down) {
        s_was_down = false;
        if (!s_long_fired) {
            return BTN_SHORT;                      /* 松手且没触发过长按 = 短按 */
        }
    }
    return BTN_NONE;
}
```

- [ ] **Step 4: 接进 main**

在 `main.c` 里实现配对流程：长按 → 扫 mDNS → 对每台没配对过的主机试 `/api/pair` →
成功则存 NVS。短按 → `hosts_cycle`。

```c
static hosts_t s_hosts;

static void do_pairing(void)
{
    ESP_LOGI(TAG, "开始配对扫描");
    host_t found[HOSTS_MAX];
    int n = discovery_scan(found, HOSTS_MAX, 3000);
    int added = 0;

    for (int i = 0; i < n; i++) {
        if (hosts_find(&s_hosts, found[i].host_id) >= 0) {
            hosts_set_addr(&s_hosts, found[i].host_id, found[i].ip, found[i].port);
            continue;                               /* 已配对，只更新地址 */
        }
        char token[40];
        if (pairing_request(found[i].ip, found[i].port, token, sizeof(token))) {
            host_t h = found[i];
            snprintf(h.token, sizeof(h.token), "%s", token);
            if (hosts_upsert(&s_hosts, &h)) {
                added++;
                ESP_LOGI(TAG, "配对成功: %s", h.name);
            }
        }
    }
    if (added > 0 || n > 0) {
        hosts_nvs_save(&s_hosts);
    }
    ESP_LOGI(TAG, "配对结束，新增 %d 台，共 %d 台", added, s_hosts.count);
}
```

主循环里：

```c
    buttons_init();
    hosts_nvs_load(&s_hosts);

    while (1) {
        btn_event_t e = buttons_poll();
        if (e == BTN_LONG) {
            do_pairing();
        } else if (e == BTN_SHORT) {
            hosts_cycle(&s_hosts);
            const host_t *c = hosts_current(&s_hosts);
            ESP_LOGI(TAG, "切换到 %s", c ? c->name : "(无)");
        }
        vTaskDelay(pdMS_TO_TICKS(50));
    }
```

- [ ] **Step 5: 编译**

Run: `idf.py build`
Expected: 编译通过

- [ ] **Step 6: 实机验证（需要用户）**

1. 宿主机托盘点「配对新设备（60 秒）」
2. **60 秒内**长按板子 GPIO21 三秒
3. 串口应打印 `配对成功: USER-20220210RC`，宿主机弹出「刚配对了一台设备」气泡
4. 拔电重插，串口应打印 `载入 1 台已配对主机`
5. 窗口关闭后再长按，串口应打印 `返回 403（配对窗口没开？）`

- [ ] **Step 7: Commit**

把 `pairing.c` `hosts_nvs.c` `buttons.c` 加进 `SRCS`，`REQUIRES` 再补上
`esp_http_client driver esp_timer`，然后 `idf.py reconfigure`。

```bash
git add amo-display/main/pairing.* amo-display/main/hosts_nvs.* amo-display/main/buttons.* amo-display/main/CMakeLists.txt amo-display/main/main.c
git commit -m "feat: 配对流程、NVS 持久化与 GPIO21 长短按"
```

---

### Task 8: 轮询与聚合视图

**Files:**
- Create: `amo-display/main/poller.h`, `amo-display/main/poller.c`
- Create: `amo-display/host_test/test_view.c`
- Modify: `amo-display/host_test/Makefile`、`amo-display/main/CMakeLists.txt`

**Interfaces:**
- Consumes: `tiny_parse.h`、`hosts.h`
- Produces:
  - `typedef enum { VS_NOLINK, VS_IDLE, VS_RUNNING, VS_WAITING, VS_STALE } view_state_t;`
  - `typedef struct { view_state_t state; char host[32]; char name[64]; char detail[80]; int idle_count; int cpu, mem, gpu, net_kb; bool peer_needs_you; char peer_name[32]; } view_t;`
  - `void view_build(const hosts_t *h, const tiny_t *samples, const bool *ok, view_t *out);` —— 纯函数
  - `void poller_start(hosts_t *h);` —— 起后台任务
  - `bool poller_take(view_t *out);` —— 取最新视图

`view_build` 是纯函数，因此"该显示哪个状态""要不要出偷看标记"这些判定能在主机上测。

- [ ] **Step 1: 写失败的测试**

`amo-display/host_test/test_view.c`：

```c
#include "greatest.h"
#include "../main/poller.h"
#include <stdio.h>

static void mk_host(hosts_t *h, const char *id, const char *name)
{
    host_t x = {0};
    snprintf(x.host_id, sizeof(x.host_id), "%s", id);
    snprintf(x.name, sizeof(x.name), "%s", name);
    snprintf(x.ip, sizeof(x.ip), "1.2.3.4");
    x.port = 8787;
    hosts_upsert(h, &x);
}

static void test_no_hosts_is_nolink(void) {
    hosts_t h; hosts_init(&h);
    view_t v;
    view_build(&h, NULL, NULL, &v);
    CHECK_INT(v.state, VS_NOLINK);
}

static void test_all_unreachable_is_nolink(void) {
    hosts_t h; hosts_init(&h);
    mk_host(&h, "id0", "A");
    tiny_t s[1] = {0};
    bool ok[1] = { false };
    view_t v;
    view_build(&h, s, ok, &v);
    CHECK_INT(v.state, VS_NOLINK);
}

static void test_waiting_wins_on_current_host(void) {
    hosts_t h; hosts_init(&h);
    mk_host(&h, "id0", "A");
    tiny_t s[1] = {0};
    s[0].waiting = 1; s[0].running = 2;
    snprintf(s[0].name, sizeof(s[0].name), "恐龙公园初始化");
    snprintf(s[0].detail, sizeof(s[0].detail), "Bash");
    bool ok[1] = { true };
    view_t v;
    view_build(&h, s, ok, &v);
    CHECK_INT(v.state, VS_WAITING);
    CHECK_STR(v.name, "恐龙公园初始化");
    CHECK_STR(v.detail, "Bash");
}

static void test_running_when_no_waiting(void) {
    hosts_t h; hosts_init(&h);
    mk_host(&h, "id0", "A");
    tiny_t s[1] = {0};
    s[0].running = 1;
    bool ok[1] = { true };
    view_t v;
    view_build(&h, s, ok, &v);
    CHECK_INT(v.state, VS_RUNNING);
}

static void test_idle_shows_count(void) {
    hosts_t h; hosts_init(&h);
    mk_host(&h, "id0", "A");
    tiny_t s[1] = {0};
    s[0].idle = 3;
    bool ok[1] = { true };
    view_t v;
    view_build(&h, s, ok, &v);
    CHECK_INT(v.state, VS_IDLE);
    CHECK_INT(v.idle_count, 3);
}

static void test_peek_flags_other_host_waiting(void) {
    /* 盯着 A，B 有人在等 —— 右边缘要出标记，否则切错机器就漏了提醒 */
    hosts_t h; hosts_init(&h);
    mk_host(&h, "id0", "A");
    mk_host(&h, "id1", "B");
    tiny_t s[2] = {0};
    s[0].idle = 1;              /* A 闲着 */
    s[1].waiting = 1;           /* B 在等 */
    bool ok[2] = { true, true };
    view_t v;
    view_build(&h, s, ok, &v);
    CHECK_INT(v.state, VS_IDLE);
    CHECK(v.peer_needs_you, "另一台在等，应出偷看标记");
    CHECK_STR(v.peer_name, "B");
}

static void test_no_peek_when_current_host_is_the_waiting_one(void) {
    hosts_t h; hosts_init(&h);
    mk_host(&h, "id0", "A");
    mk_host(&h, "id1", "B");
    tiny_t s[2] = {0};
    s[0].waiting = 1;
    s[1].idle = 1;
    bool ok[2] = { true, true };
    view_t v;
    view_build(&h, s, ok, &v);
    CHECK_INT(v.state, VS_WAITING);
    CHECK(!v.peer_needs_you, "自己就在等，不该再出别人的标记");
}

static void test_metrics_come_from_current_host(void) {
    hosts_t h; hosts_init(&h);
    mk_host(&h, "id0", "A");
    mk_host(&h, "id1", "B");
    tiny_t s[2] = {0};
    s[0].cpu = 11; s[0].mem = 22; s[0].gpu = -1; s[0].net_kb = 44;
    s[1].cpu = 99;
    bool ok[2] = { true, true };
    view_t v;
    view_build(&h, s, ok, &v);
    CHECK_INT(v.cpu, 11);
    CHECK_INT(v.gpu, -1);
}

int main(void) {
    printf("view:\n");
    test_no_hosts_is_nolink();
    test_all_unreachable_is_nolink();
    test_waiting_wins_on_current_host();
    test_running_when_no_waiting();
    test_idle_shows_count();
    test_peek_flags_other_host_waiting();
    test_no_peek_when_current_host_is_the_waiting_one();
    test_metrics_come_from_current_host();
    REPORT();
}
```

`Makefile` 增加：

```makefile
test_view: test_view.c ../main/poller.c ../main/hosts.c
	$(CC) $(CFLAGS) -DPOLLER_HOST_TEST -o $@ $^
```

并把 `test_view` 加进 `all` 的依赖与执行列表。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd amo-display\host_test && make`
Expected: `poller.h: No such file or directory`

- [ ] **Step 3: 写实现**

`amo-display/main/poller.h`：

```c
#pragma once
#include "hosts.h"
#include "tiny_parse.h"
#include <stdbool.h>

typedef enum { VS_NOLINK, VS_IDLE, VS_RUNNING, VS_WAITING, VS_STALE } view_state_t;

typedef struct {
    view_state_t state;
    char host[32];
    char name[64];
    char detail[80];
    int  idle_count;
    int  cpu, mem, gpu, net_kb;
    bool peer_needs_you;        /* 另一台已配对主机有人在等 */
    char peer_name[32];
} view_t;

/* 纯函数：由所有主机的采样合成当前该显示什么。
   samples/ok 的下标与 h->items 一一对应；h->count 为 0 时两者可为 NULL。 */
void view_build(const hosts_t *h, const tiny_t *samples, const bool *ok, view_t *out);

#ifndef POLLER_HOST_TEST
void poller_start(hosts_t *h);
bool poller_take(view_t *out);
#endif
```

`amo-display/main/poller.c` 的纯函数部分：

```c
#include "poller.h"
#include <string.h>
#include <stdio.h>

void view_build(const hosts_t *h, const tiny_t *samples, const bool *ok, view_t *out)
{
    memset(out, 0, sizeof(*out));
    out->gpu = -1;

    if (!h || h->count <= 0 || !samples || !ok) {
        out->state = VS_NOLINK;
        return;
    }

    int cur = h->current;
    if (cur < 0 || cur >= h->count || !ok[cur]) {
        out->state = VS_NOLINK;             /* 当前这台够不着就是够不着，别假装正常 */
        return;
    }

    const tiny_t *s = &samples[cur];
    snprintf(out->host, sizeof(out->host), "%s", h->items[cur].name);
    snprintf(out->name, sizeof(out->name), "%s", s->name);
    snprintf(out->detail, sizeof(out->detail), "%s", s->detail);
    out->idle_count = s->idle;
    out->cpu = s->cpu;
    out->mem = s->mem;
    out->gpu = s->gpu;
    out->net_kb = s->net_kb;

    if (s->waiting > 0) {
        out->state = VS_WAITING;
    } else if (s->running > 0) {
        out->state = VS_RUNNING;
    } else {
        out->state = VS_IDLE;
    }

    /* 偷看：盯着这台的时候，别台有人在等也要能看见 —— 否则切错机器就漏提醒。
       自己已经在 waiting 就不必再标别人。 */
    if (out->state != VS_WAITING) {
        for (int i = 0; i < h->count; i++) {
            if (i == cur || !ok[i]) {
                continue;
            }
            if (samples[i].waiting > 0) {
                out->peer_needs_you = true;
                snprintf(out->peer_name, sizeof(out->peer_name), "%s", h->items[i].name);
                break;
            }
        }
    }
}
```

同文件里、`#ifndef POLLER_HOST_TEST` 保护的后台任务部分：

```c
#ifndef POLLER_HOST_TEST

#include "freertos/FreeRTOS.h"
#include "freertos/task.h"
#include "freertos/semphr.h"
#include "esp_http_client.h"
#include "esp_log.h"

static const char *TAG = "poll";
static hosts_t *s_hosts;
static view_t   s_view;
static SemaphoreHandle_t s_lock;

static bool fetch_tiny(const host_t *h, tiny_t *out)
{
    char url[128];
    snprintf(url, sizeof(url), "http://%s:%d/api/tiny?k=%s%s",
             h->ip, h->port, h->token,
#ifdef CONFIG_AGENT_TINY_FULL_DETAIL
             "&d=full"
#else
             ""
#endif
    );

    esp_http_client_config_t cfg = { .url = url, .timeout_ms = 2000 };
    esp_http_client_handle_t c = esp_http_client_init(&cfg);
    bool ok = false;

    if (esp_http_client_open(c, 0) == ESP_OK) {
        esp_http_client_fetch_headers(c);
        if (esp_http_client_get_status_code(c) == 200) {
            char buf[320] = {0};
            int n = esp_http_client_read_response(c, buf, sizeof(buf) - 1);
            if (n > 0) {
                ok = tiny_parse(buf, out);
            }
        }
    }
    esp_http_client_cleanup(c);
    return ok;
}

static void poll_task(void *arg)
{
    (void)arg;
    tiny_t samples[HOSTS_MAX];
    bool   ok[HOSTS_MAX];

    for (;;) {
        for (int i = 0; i < s_hosts->count; i++) {
            ok[i] = fetch_tiny(&s_hosts->items[i], &samples[i]);
            s_hosts->items[i].online = ok[i];
        }
        view_t v;
        view_build(s_hosts, samples, ok, &v);

        xSemaphoreTake(s_lock, portMAX_DELAY);
        s_view = v;
        xSemaphoreGive(s_lock);

        vTaskDelay(pdMS_TO_TICKS(1000));
    }
}

void poller_start(hosts_t *h)
{
    s_hosts = h;
    s_lock = xSemaphoreCreateMutex();
    xTaskCreate(poll_task, "poll", 6144, NULL, 4, NULL);
}

bool poller_take(view_t *out)
{
    if (xSemaphoreTake(s_lock, pdMS_TO_TICKS(50)) != pdTRUE) {
        return false;
    }
    *out = s_view;
    xSemaphoreGive(s_lock);
    return true;
}

#endif /* POLLER_HOST_TEST */
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd amo-display\host_test && make`
Expected: 三个测试全部 `0 failed`

- [ ] **Step 5: 编译固件**

Run: `idf.py build`
Expected: 编译通过

- [ ] **Step 6: Commit**

```bash
git add amo-display/main/poller.* amo-display/host_test/ amo-display/main/CMakeLists.txt
git commit -m "feat: 轮询全部已配对主机并合成视图，偷看标记按身份判定"
```

---

### Task 9: 横屏 UI 与告警

**Files:**
- Create: `amo-display/components/agent_ui/ui.h`, `amo-display/components/agent_ui/ui.c`
- Create: `amo-display/components/agent_ui/CMakeLists.txt`
- Create: `amo-display/main/led.h`, `amo-display/main/led.c`
- Modify: `amo-display/main/main.c`

**Interfaces:**
- Consumes: `poller.h` 的 `view_t`、`ui_strings.h`、字库符号
- Produces:
  - `void ui_init(void);`
  - `void ui_render(const view_t *v);` —— 幂等，同状态重复调用不重建控件
  - `void led_init(void);` / `void led_set_waiting(bool on);`

- [ ] **Step 1: 组件声明**

`amo-display/components/agent_ui/CMakeLists.txt`：

```cmake
idf_component_register(
    SRCS "ui.c" "fonts/font_status_28.c" "fonts/font_cjk_16.c"
    INCLUDE_DIRS "."
    REQUIRES lvgl lilygo_board
)
```

- [ ] **Step 2: UI 实现**

`amo-display/components/agent_ui/ui.h`：

```c
#pragma once
#include "poller.h"

void ui_init(void);
void ui_render(const view_t *v);
```

`amo-display/components/agent_ui/ui.c` —— 横屏 368×194，左侧色块承担状态、右侧承担内容：

```c
#include "ui.h"
#include "ui_strings.h"
#include "lvgl.h"
#include "amoled_driver.h"
#include <stdio.h>

LV_FONT_DECLARE(lv_font_status_28);
LV_FONT_DECLARE(lv_font_cjk_16);

/* 与平板 / 托盘完全相同的语义色 */
#define C_RUN    lv_color_hex(0x3FBFD8)
#define C_WAIT   lv_color_hex(0xFFB020)
#define C_IDLE   lv_color_hex(0x4A5B78)
#define C_STALE  lv_color_hex(0xE2564A)
#define C_NOLINK lv_color_hex(0x1E3A5F)

#define W 368
#define H 194

static lv_obj_t *s_bar;        /* 左侧竖色块 */
static lv_obj_t *s_state;      /* 状态词，28px */
static lv_obj_t *s_name;       /* 会话名，16px */
static lv_obj_t *s_detail;     /* detail，16px */
static lv_obj_t *s_peek;       /* 右边缘偷看标记 */
static lv_obj_t *s_big;        /* 空闲态大数字 */
static lv_obj_t *s_metrics;    /* 空闲态系统指标 */
static lv_anim_t s_breathe;

static view_state_t s_last = -1;

static void breathe_cb(void *obj, int32_t v)
{
    lv_obj_set_style_bg_opa((lv_obj_t *)obj, (lv_opa_t)v, 0);
}

void ui_init(void)
{
    lv_obj_t *scr = lv_scr_act();
    lv_obj_set_style_bg_color(scr, lv_color_black(), 0);
    lv_obj_clear_flag(scr, LV_OBJ_FLAG_SCROLLABLE);

    s_bar = lv_obj_create(scr);
    lv_obj_set_style_border_width(s_bar, 0, 0);
    lv_obj_set_style_radius(s_bar, 0, 0);
    lv_obj_set_pos(s_bar, 0, 0);

    s_state = lv_label_create(scr);
    lv_obj_set_style_text_font(s_state, &lv_font_status_28, 0);

    s_name = lv_label_create(scr);
    lv_obj_set_style_text_font(s_name, &lv_font_cjk_16, 0);
    lv_obj_set_width(s_name, 250);
    lv_label_set_long_mode(s_name, LV_LABEL_LONG_DOT);

    s_detail = lv_label_create(scr);
    lv_obj_set_style_text_font(s_detail, &lv_font_cjk_16, 0);
    lv_obj_set_width(s_detail, 250);
    lv_label_set_long_mode(s_detail, LV_LABEL_LONG_DOT);

    s_peek = lv_label_create(scr);
    lv_obj_set_style_text_font(s_peek, &lv_font_cjk_16, 0);
    lv_obj_set_style_text_color(s_peek, C_WAIT, 0);
    lv_obj_align(s_peek, LV_ALIGN_BOTTOM_RIGHT, -8, -6);
    lv_obj_add_flag(s_peek, LV_OBJ_FLAG_HIDDEN);

    s_big = lv_label_create(scr);
    lv_obj_set_style_text_font(s_big, &lv_font_montserrat_48, 0);
    lv_obj_set_style_text_color(s_big, C_IDLE, 0);

    s_metrics = lv_label_create(scr);
    lv_obj_set_style_text_font(s_metrics, &lv_font_montserrat_14, 0);
    lv_obj_set_style_text_color(s_metrics, lv_color_hex(0x2A3648), 0);
}

static void layout_alert(lv_color_t c, const char *word)
{
    /* 等待态：左侧 88px 大色块 —— 发光面积编码状态，两米外靠亮度而不是图标细节 */
    lv_obj_set_size(s_bar, 88, H);
    lv_obj_set_style_bg_color(s_bar, c, 0);
    lv_obj_set_style_bg_opa(s_bar, LV_OPA_COVER, 0);

    lv_obj_clear_flag(s_state, LV_OBJ_FLAG_HIDDEN);
    lv_obj_set_style_text_color(s_state, c, 0);
    lv_label_set_text(s_state, word);
    lv_obj_align(s_state, LV_ALIGN_TOP_LEFT, 104, 22);

    lv_obj_add_flag(s_big, LV_OBJ_FLAG_HIDDEN);
    lv_obj_add_flag(s_metrics, LV_OBJ_FLAG_HIDDEN);
    lv_obj_clear_flag(s_name, LV_OBJ_FLAG_HIDDEN);
    lv_obj_clear_flag(s_detail, LV_OBJ_FLAG_HIDDEN);
    lv_obj_align(s_name, LV_ALIGN_TOP_LEFT, 104, 66);
    lv_obj_align(s_detail, LV_ALIGN_TOP_LEFT, 104, 96);
}

static void layout_quiet(lv_color_t c, const char *word)
{
    /* 运行/失联：左侧只留 6px 竖条，安静 */
    lv_obj_set_size(s_bar, 6, H);
    lv_obj_set_style_bg_color(s_bar, c, 0);
    lv_obj_set_style_bg_opa(s_bar, LV_OPA_COVER, 0);

    lv_obj_clear_flag(s_state, LV_OBJ_FLAG_HIDDEN);
    lv_obj_set_style_text_color(s_state, c, 0);
    lv_label_set_text(s_state, word);
    lv_obj_align(s_state, LV_ALIGN_TOP_LEFT, 22, 22);

    lv_obj_add_flag(s_big, LV_OBJ_FLAG_HIDDEN);
    lv_obj_add_flag(s_metrics, LV_OBJ_FLAG_HIDDEN);
    lv_obj_clear_flag(s_name, LV_OBJ_FLAG_HIDDEN);
    lv_obj_clear_flag(s_detail, LV_OBJ_FLAG_HIDDEN);
    lv_obj_align(s_name, LV_ALIGN_TOP_LEFT, 22, 66);
    lv_obj_align(s_detail, LV_ALIGN_TOP_LEFT, 22, 96);
}

void ui_render(const view_t *v)
{
    bool changed = (v->state != s_last);
    if (changed) {
        lv_anim_del(s_bar, breathe_cb);
        lv_obj_set_style_bg_opa(s_bar, LV_OPA_COVER, 0);
    }

    switch (v->state) {
    case VS_WAITING:
        if (changed) {
            layout_alert(C_WAIT, UI_S_WAITING);
            amoled_set_brightness(255);
            lv_anim_init(&s_breathe);
            lv_anim_set_var(&s_breathe, s_bar);
            lv_anim_set_exec_cb(&s_breathe, breathe_cb);
            lv_anim_set_values(&s_breathe, 110, 255);
            lv_anim_set_time(&s_breathe, 700);
            lv_anim_set_playback_time(&s_breathe, 700);
            lv_anim_set_repeat_count(&s_breathe, LV_ANIM_REPEAT_INFINITE);
            lv_anim_start(&s_breathe);
        }
        lv_obj_set_style_text_color(s_name, lv_color_white(), 0);
        lv_obj_set_style_text_color(s_detail, lv_color_hex(0x8A6A2A), 0);
        break;

    case VS_RUNNING:
        if (changed) { layout_quiet(C_RUN, UI_S_RUNNING); amoled_set_brightness(60); }
        lv_obj_set_style_text_color(s_name, lv_color_hex(0x8FD9E8), 0);
        lv_obj_set_style_text_color(s_detail, lv_color_hex(0x3D6B78), 0);
        break;

    case VS_STALE:
        if (changed) { layout_quiet(C_STALE, UI_S_STALE); amoled_set_brightness(90); }
        break;

    case VS_NOLINK:
        if (changed) {
            layout_quiet(C_NOLINK, UI_S_NOLINK);
            amoled_set_brightness(30);
            lv_label_set_text(s_name, "");
            lv_label_set_text(s_detail, "");
        }
        lv_obj_add_flag(s_peek, LV_OBJ_FLAG_HIDDEN);
        s_last = v->state;
        return;                              /* 断连时不显示任何会话信息 */

    case VS_IDLE:
    default:
        if (changed) {
            /* 空闲：左半大数字，右半系统指标。AMOLED 黑像素不通电，此态近乎熄屏 */
            lv_obj_set_size(s_bar, 6, H);
            lv_obj_set_style_bg_color(s_bar, C_IDLE, 0);
            amoled_set_brightness(20);
            lv_obj_add_flag(s_name, LV_OBJ_FLAG_HIDDEN);
            lv_obj_add_flag(s_detail, LV_OBJ_FLAG_HIDDEN);
            lv_obj_clear_flag(s_big, LV_OBJ_FLAG_HIDDEN);
            lv_obj_clear_flag(s_metrics, LV_OBJ_FLAG_HIDDEN);
            lv_obj_clear_flag(s_state, LV_OBJ_FLAG_HIDDEN);
            lv_obj_set_style_text_color(s_state, C_IDLE, 0);
            lv_label_set_text(s_state, UI_S_IDLE);
            lv_obj_align(s_big, LV_ALIGN_LEFT_MID, 34, -14);
            lv_obj_align(s_state, LV_ALIGN_LEFT_MID, 30, 30);
            lv_obj_align(s_metrics, LV_ALIGN_RIGHT_MID, -24, 0);
        }
        lv_label_set_text_fmt(s_big, "%d", v->idle_count);
        break;
    }

    if (v->state != VS_IDLE) {
        lv_label_set_text(s_name, v->name);
        lv_label_set_text(s_detail, v->detail);
    } else {
        char m[96];
        if (v->gpu < 0) {
            snprintf(m, sizeof(m), "cpu %d%%   mem %d%%\ngpu  --   net %dKB/s",
                     v->cpu, v->mem, v->net_kb);
        } else {
            snprintf(m, sizeof(m), "cpu %d%%   mem %d%%\ngpu %d%%   net %dKB/s",
                     v->cpu, v->mem, v->gpu, v->net_kb);
        }
        lv_label_set_text(s_metrics, m);
    }

    if (v->peer_needs_you) {
        lv_label_set_text_fmt(s_peek, "%s %s", v->peer_name, UI_S_WAITING);
        lv_obj_clear_flag(s_peek, LV_OBJ_FLAG_HIDDEN);
    } else {
        lv_obj_add_flag(s_peek, LV_OBJ_FLAG_HIDDEN);
    }

    s_last = v->state;
}
```

- [ ] **Step 3: WS2812 告警**

本板没有蜂鸣器，告警靠 LED + 屏幕亮度。

`amo-display/main/led.h`：

```c
#pragma once
#include <stdbool.h>

void led_init(void);
void led_set_waiting(bool on);   /* 进入等待态点亮琥珀，离开熄灭 */
```

`amo-display/main/led.c`：

```c
#include "led.h"
#include "driver/rmt_tx.h"
#include "led_strip.h"
#include "esp_log.h"

#define LED_GPIO 18            /* 本板 WS2812 在 GPIO18，1 颗 */

static led_strip_handle_t s_strip;

void led_init(void)
{
    led_strip_config_t cfg = {
        .strip_gpio_num = LED_GPIO,
        .max_leds = 1,
    };
    led_strip_rmt_config_t rmt = {
        .resolution_hz = 10 * 1000 * 1000,
    };
    ESP_ERROR_CHECK(led_strip_new_rmt_device(&cfg, &rmt, &s_strip));
    led_strip_clear(s_strip);
}

void led_set_waiting(bool on)
{
    if (on) {
        led_strip_set_pixel(s_strip, 0, 255, 176, 32);   /* #FFB020，与三端同色 */
        led_strip_refresh(s_strip);
    } else {
        led_strip_clear(s_strip);
    }
}
```

`main/idf_component.yml` 增加 `espressif/led_strip: "^2.5.0"`。

- [ ] **Step 4: 主循环接起来**

```c
    ui_init();
    led_init();
    poller_start(&s_hosts);

    view_state_t prev = VS_NOLINK;
    int64_t last_shift = 0;

    while (1) {
        btn_event_t e = buttons_poll();
        if (e == BTN_LONG)  { do_pairing(); }
        if (e == BTN_SHORT) { hosts_cycle(&s_hosts); hosts_nvs_save(&s_hosts); }

        view_t v;
        if (poller_take(&v)) {
            ui_render(&v);
            /* 告警只在"进入"等待态那一次触发，不在持续期间反复 */
            if (v.state == VS_WAITING && prev != VS_WAITING) {
                led_set_waiting(true);
            } else if (v.state != VS_WAITING && prev == VS_WAITING) {
                led_set_waiting(false);
            }
            prev = v.state;
        }

        /* 防烧屏：每 60 秒整体偏移 1px */
        int64_t now = esp_timer_get_time();
        if (now - last_shift > 60LL * 1000 * 1000) {
            static int8_t d = 0;
            d = (int8_t)((d + 1) % 3);
            lv_obj_set_y(lv_scr_act(), d - 1);
            last_shift = now;
        }

        lv_timer_handler();
        vTaskDelay(pdMS_TO_TICKS(20));
    }
```

- [ ] **Step 5: 编译**

先把 `poller.c` `led.c` 加进 `SRCS`，`REQUIRES` 补上 `lvgl agent_ui espressif__led_strip`，
再 `idf.py reconfigure`。

Run: `idf.py build`
Expected: 编译通过。若报 `undefined reference to lv_font_montserrat_48`，
去 `sdkconfig.defaults` 确认 `CONFIG_LV_FONT_MONTSERRAT_48=y` 后 `idf.py reconfigure`。

- [ ] **Step 6: 实机验证（需要用户）**

烧录后逐条确认：

1. 无已配对主机 → 慢速蓝 `连不上`，亮度很低
2. 配对后宿主空闲 → 左侧 6px 暗条 + 大数字会话数 + 右半系统指标，屏幕近乎全黑
3. 宿主有会话运行 → 青色竖条 + `干着呢` + 会话名 + 工具名
4. 触发一次权限询问 → 左侧整块 88px 琥珀色呼吸 + 屏幕拉满 + WS2812 亮琥珀
5. 批准之后 → LED 熄灭，回到运行态
6. 两台主机都配对，盯着 A 而 B 有人在等 → 右下角出现 `B 该你了`
7. 拔掉宿主机网线 → 变成 `连不上` 而不是停在最后画面

- [ ] **Step 7: 缺字形的兜底**

`font_cjk_16` 只覆盖 ASCII + GB2312 一级 3755 字，且 `.fallback = NULL`。
LVGL 对字库里没有的码点**什么都不画** —— 没有方框、没有日志、没有任何提示，
屏幕上只是一段看不见的空隙。繁体字、生僻姓氏、商标名、emoji 都会触发。

在 `ui_render` 写入 `s_name` / `s_detail` 之前先过一遍替换：把不在字库里的
码点换成占位符（字库里已预留该字形）。规范的替换字符是 U+FFFD，但思源黑体 SC
没有这个字形（用 fontTools 的 `getBestCmap()` 核实过）；`gen_fonts.ps1` 改用
**U+25A1（□ 白方块）**代替，`font_cjk_16` 里已经生成了这个字形。最简做法是维护
一个"已知可渲染区间"的判断函数：ASCII 0x20-0x7F、GB2312 一级对应的 Unicode
集合、以及 U+25A1 本身。

**实机验证**：把某台机器的会话名临时改成含繁体字或 emoji 的名字，
屏幕上应显示 `□` 而不是空白。

- [ ] **Step 8: Commit**

```bash
git add amo-display/components/agent_ui/ amo-display/main/led.* amo-display/main/main.c amo-display/main/idf_component.yml
git commit -m "feat: 横屏三态 UI、WS2812 告警与防烧屏偏移"
```

---

### Task 10: 清理与文档

**Files:**
- Delete: `agent_display.ino`
- Modify: `amo-display/README.md`、`README.md`、`CLAUDE.md`

**Interfaces:**
- Consumes: 全部前序任务
- Produces: 无

- [ ] **Step 1: 删掉旧骨架**

`agent_display.ino` 是重写前的产物，且**有害**：它的 `BUZZER_PIN 2` 是本板 I2C SCL，
屏驱注释写成 RM67162（实为 SH8501B0），轮询 `/api/tiny` 不带令牌会 401。
留着只会让人照抄。

```bash
git rm agent_display.ino
```

- [ ] **Step 2: 补 `amo-display/README.md`**

在 Task 0 已写的构建环境之后补上使用说明：

```markdown
## 首次使用

1. `idf.py menuconfig` → Agent Display → 填 WiFi SSID 与密码
2. `idf.py -p COM<n> flash monitor`
3. 宿主机托盘点「配对新设备（60 秒）」
4. 60 秒内长按板子 GPIO21 三秒，串口打印「配对成功」即可

## 按键

| 操作 | 行为 |
|---|---|
| 短按 GPIO21 | 在已配对主机间循环切换 |
| 长按 GPIO21 三秒 | 扫描并配对新主机 |

## 硬件注意

本板**没有蜂鸣器**，告警靠 WS2812（GPIO18）与屏幕亮度。
要外接无源蜂鸣器就在 menuconfig 里填一个空闲脚，建议 GPIO17。
**绝对不要用 GPIO2** —— 那是本板 I2C SCL，会打死 AXP2101 电源管理和 CHSC5816 触摸。
```

- [ ] **Step 3: 更新根 `README.md`**

把「后续可以加的」里关于 ESP32 的表述改成已实现，并指向 `amo-display/README.md`。

- [ ] **Step 4: 更新 `CLAUDE.md`**

改掉两处现在已经不成立的说法：

- 「Firmware: `agent_display.ino` is the pre-rewrite skeleton」→ 改为 `amo-display/` 是当前固件，`.ino` 已删除
- Roadmap 里「The ESP32 firmware is a separate plan, not yet written」→ 改为已完成，指向本计划

并在 Commands 一节补上固件构建与主机端纯逻辑测试：

```powershell
cd amo-display\host_test && make      # 纯逻辑测试，不需要板子
cd amo-display && idf.py build        # 固件，需要先 export.ps1
```

- [ ] **Step 5: 全量测试**

Run: `python -m pytest tests/ -v`
Expected: 全绿，且 `test_firmware_copy.py` 的四条不再 skip 而是真的 PASS。

Run: `cd amo-display\host_test && make`
Expected: 三个测试全部 `0 failed`

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "docs: ESP32 固件文档与清理，删除有害的旧 .ino 骨架"
```

---

## 完成标准

- [ ] `cd amo-display\host_test && make` 三个测试全绿（解析、主机表、视图合成）
- [ ] `python -m pytest tests/ -v` 全绿，含固件文案防漂移测试
- [ ] `idf.py build` 编译通过
- [ ] 实机：长按配对成功，宿主机弹出气泡
- [ ] 实机：拔电重插后仍记得已配对主机
- [ ] 实机：四种状态显示正确，等待态左侧整块琥珀呼吸 + WS2812 亮起
- [ ] 实机：两台主机时右下角出现偷看标记
- [ ] 实机：断网变成 `连不上` 而不是停在最后画面

## 已知风险

| 风险 | 处置 |
|---|---|
| C 盘 0 字节可用，IDF 组件管理器缓存无法改盘 | Task 0 Step 1 硬性拦截，不足 3GB 就停 |
| IDF v5.2.3 vs 官方要求的 5.3.0 | Task 0 Step 5 是判据；实测 992 个 IDF 核心目标已全部编过 |
| 板子未连接，全部实机行为未验证 | 每个涉及硬件的步骤都标注「需要用户」，不假装验证过 |
| 主机 gcc 可能不存在 | Task 1 Step 3 显式检查；没有它则纯逻辑完全无法在无板情况下验证 |
| `font_cjk_16` 全 CJK 区间可能撑爆 3M app 分区 | Task 4 Step 4 检查体积，超了缩字符集而不是撑大分区 |
| `amoled_driver.h` 的实际函数签名可能与本计划假设不符 | Task 3 Step 3 明确：按头文件改 main，不改裁来的驱动 |
